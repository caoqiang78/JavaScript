在函数代码中无需指定参数，可以用arguments[i]直接访问参数。arguments[0]是第一个参数，以此类推。arguments.length可以用来检测参数个数。
function doAdd() {
  if(arguments.length == 1) {
    alert(arguments[0] + 5);
  } else if(arguments.length == 2) {
    alert(arguments[0] + arguments[1]);
  }
}

doAdd(10);	//输出 "15"
doAdd(40, 20);	//输出 "60"
